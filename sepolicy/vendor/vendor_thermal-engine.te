allow vendor_thermal-engine audio_device:chr_file rw_file_perms;

allow vendor_thermal-engine self:capability kill;

allow vendor_thermal-engine property_socket:sock_file write;
allow vendor_thermal-engine init:unix_stream_socket connectto;

get_prop(vendor_thermal-engine, vendor_mpctl_prop)
